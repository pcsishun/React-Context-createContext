import React from 'react'

const HeaderTitle = () => {
    const headername = "Title useContext"
    return (
        <div>
            <h1>{headername}</h1>
        </div>
    )
}

export default HeaderTitle;